// import { render } from '@testing-library/svelte';
// import AdaptiveInfo from '../AdaptiveInfo.svelte';

describe('Component', () => {
  test('Should render skeleton correctly', () => {
    // const { container } = render(App({ target: document.body }));
    // expect(container).toContainHTML('<main><header></header><div></div></main>');
  });
});
